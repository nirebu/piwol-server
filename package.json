{
	"name": "piwol-server",
	"version": "1.0.0",
	"description": "A Wake on Lan server designed to run on a Raspberry Pi",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev": "nodemon --ignore db.json index.js"
	},
	"keywords": [
		"Wake on lan",
		"raspberry pi",
		"server"
	],
	"author": "Nicolò Rebughini",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"dotenv": "^8.1.0",
		"express": "^4.18.2",
		"local-devices": "^2.0.0",
		"lowdb": "^1.0.0",
		"node-arp": "^1.0.6",
		"node-wol": "^0.1.1",
		"ping": "^0.2.2"
	},
	"devDependencies": {
		"nodemon": "^1.19.1"
	}
}
